var express = require('express');
var router = express.Router();

const isLegalMove = (player, type, src, dest) => {
    


}

router.post('/:move', function (req,res,next) {
    if (move = legalMove) {
        currentPos = dest
    }
    else {
        return;
    }


})
